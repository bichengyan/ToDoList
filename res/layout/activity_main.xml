<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ListView
        android:id="@+id/todoItemListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/emailItemButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" >

    </ListView>

    <Button
        android:id="@+id/removeItemButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/finishItemButton"
        android:onClick="removeItem"
        android:text="@string/remove" />

    <Button
        android:id="@+id/finishItemButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/removeItemButton"
        android:layout_alignBottom="@+id/removeItemButton"
        android:layout_alignLeft="@+id/todoItemListView"
        android:onClick="finishItem"
        android:text="@string/finish" />

    <Button
        android:id="@+id/emailItemButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/todoItemListView"
        android:onClick="emailItem"
        android:text="@string/email" />

    <Button
        android:id="@+id/archiveItemButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/emailItemButton"
        android:layout_toRightOf="@+id/removeItemButton"
        android:onClick="archiveItem"
        android:text="@string/archive" />

</RelativeLayout>